apiVersion: v1
kind: Service
metadata:
  name: infra-eye
  namespace: infra-eye
spec:
  type: NodePort
  selector:
    app: infra-eye
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080   # Access at http://<node-ip>:30080
